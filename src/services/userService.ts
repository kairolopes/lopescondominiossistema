// Placeholder for User Service logic (e.g., database integration)
export const userService = {
    findByPhone: async (phone: string) => {
        // Mock
        return { phone, name: 'Condômino' };
    }
};
